I can see that the backbone module uses the operation manager's log() method to redirect logs to the log_accordion. This is different from the suppression approach. Let me check how the colab module is currently structured and then implement the same log redirection pattern.
    
Now I'll implement the log redirection pattern from the backbone module. I need to add a method similar to the train module's _log_initialization_complete() that redirects initial logs to the log_accordion after the operation container is created. Let me first find where the operation manager is initialized in the colab module.

Now I need to also add the log suppression decorator to the colab module's initialize method, similar to what we have in the train module. 